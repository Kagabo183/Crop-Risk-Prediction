import React from 'react';
import './Header.css';

const Header = () => (
  <header className="header">
    <h1>Crop Risk Dashboard</h1>
    <div className="header-user">Welcome, User</div>
  </header>
);

export default Header;
